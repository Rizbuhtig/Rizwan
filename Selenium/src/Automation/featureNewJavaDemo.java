package Automation;

import org.openqa.selenium.By;

import org.openqa.selenium.WebDriver;

import org.openqa.selenium.WebElement;

import org.openqa.selenium.chrome.ChromeDriver;
import org.openqa.selenium.firefox.FirefoxDriver;

//import static org.openqa.selenium.support.locators.RelativeLocator.withTagName;


public class featureNewJavaDemo {

	public static void main(String[] args) throws InterruptedException {
		// TODO Auto-generated method stub
		
		// Add all 4.0 java jars and remove the comments. So, the code works
		
		
		/*System.setProperty("webdriver.gecko.driver", "D:/geckodriver/geckodriver.exe");
		WebDriver driver = new FirefoxDriver();
		
		driver.get("https://rahulshettyacademy.com/angularpractice/");
		Thread.sleep(5000);
		
		WebElement namebox = driver.findElement(By.cssSelector("[name='name']"));
		System.out.println(driver.findElement(withTagName("label").above(namebox)).getText());
		
		
		WebElement dateofbirth = driver.findElement(By.cssSelector("for='dateofBirth"));
		System.out.println(driver.findElement(withTagName("input").below(dateofbirth)).sendkeys("02/09/1991"));
		 
		WebElement checkbox = driver.findElement(By.xpath("//label[text()='Check me out if you Love Ice Creams!']"));
		driver.findElement(withTagName("input").toLeftOf(checkbox)).click();
		
		WebElement empstatus = driver.findElement(By.xpath("//label[text()='Employment Status']"));
		driver.findElement(withTagName("input").toRightof(empstatus)).click();*/
	}

	

}
